#
# Copyright (C) 2015 Wind River Systems, Inc.
#

# We have a conf and classes directory, add to BBPATH
BBPATH =. "${LAYERDIR}:"

# We have a packages directory, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
 	${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "axxiaarm"
BBFILE_PATTERN_axxiaarm = "^${LAYERDIR}/"
BBFILE_PRIORITY_axxiaarm = "7"

# Let the "No recipes available for: pkg.bbapeend" be WARNINGS until we
# cleanup the unused bbappend.
BB_DANGLINGAPPENDS_WARNONLY ?= "true"
# This should only be incremented on significant changes that will
# cause compatibility issues with other layers
LAYERVERSION_axxiaarm = "1"

LAYERDEPENDS_axxiaarm = "core wr-kernel"
